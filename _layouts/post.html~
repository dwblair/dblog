---
layout: default
---
 
<div class="meta"><span>{{ page.date | date_to_string }}</span> <b><a href="{{ page.url }}">{{ page.title }}</a> </b> {{ page.summary }}</div>

<div class="post">
{{ content }}

</div>

