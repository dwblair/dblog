---
layout: default
title: Don Blair's Stuff 
---
<div id="home">
  <ul class="posts">
    {% for post in site.posts %}
      <li>
  
       <span>{{ post.date | date_to_string }}</span>  <b><a href="{{ post.url }}">{{ post.title }}</b></a> 
       
       <!--    <img src = {{ post.pic }} width=50 align:left>   -->
       
       {{ post.summary }} </li>
    {% endfor %}
  </ul>
  
</div>
